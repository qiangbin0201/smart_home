package com.afa.tourism.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.smart.home.model.EquipData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "EQUIP_DATA".
*/
public class EquipDataDao extends AbstractDao<EquipData, Long> {

    public static final String TABLENAME = "EQUIP_DATA";

    /**
     * Properties of entity EquipData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property EquipName = new Property(1, String.class, "equipName", false, "EQUIP_NAME");
        public final static Property EquipPosition = new Property(2, String.class, "equipPosition", false, "EQUIP_POSITION");
        public final static Property EquipCode = new Property(3, String.class, "equipCode", false, "EQUIP_CODE");
    }


    public EquipDataDao(DaoConfig config) {
        super(config);
    }
    
    public EquipDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"EQUIP_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"EQUIP_NAME\" TEXT," + // 1: equipName
                "\"EQUIP_POSITION\" TEXT," + // 2: equipPosition
                "\"EQUIP_CODE\" TEXT);"); // 3: equipCode
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"EQUIP_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, EquipData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String equipName = entity.getEquipName();
        if (equipName != null) {
            stmt.bindString(2, equipName);
        }
 
        String equipPosition = entity.getEquipPosition();
        if (equipPosition != null) {
            stmt.bindString(3, equipPosition);
        }
 
        String equipCode = entity.getEquipCode();
        if (equipCode != null) {
            stmt.bindString(4, equipCode);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, EquipData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String equipName = entity.getEquipName();
        if (equipName != null) {
            stmt.bindString(2, equipName);
        }
 
        String equipPosition = entity.getEquipPosition();
        if (equipPosition != null) {
            stmt.bindString(3, equipPosition);
        }
 
        String equipCode = entity.getEquipCode();
        if (equipCode != null) {
            stmt.bindString(4, equipCode);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public EquipData readEntity(Cursor cursor, int offset) {
        EquipData entity = new EquipData( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // equipName
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // equipPosition
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // equipCode
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, EquipData entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setEquipName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setEquipPosition(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setEquipCode(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(EquipData entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(EquipData entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(EquipData entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
